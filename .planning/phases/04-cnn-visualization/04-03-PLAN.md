---
phase: 04-cnn-visualization
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - frontend/app/page.js
  - frontend/app/components/PredictionSection.js
  - frontend/app/components/SkeletonLoaders.js
autonomous: false
requirements: [VIS-04, VIS-05]

must_haves:
  truths:
    - "Skeleton loaders with shimmer animation appear while inference is running for heatmaps, confidence chart, and architecture diagram areas"
    - "When backend is unreachable, per-section error fallbacks appear with dashed borders and retry option"
    - "After inference completes, all visualizations (heatmaps, chart, diagram) render with real data"
    - "Page layout flows: Header -> Error banner (if any) -> Image picker -> Architecture Diagram -> Prediction + Confidence Chart -> Activation Heatmaps"
  artifacts:
    - path: "frontend/app/components/SkeletonLoaders.js"
      provides: "Skeleton placeholder components matching visualization section dimensions"
      exports: ["SkeletonHeatmapGrid", "SkeletonBarChart"]
    - path: "frontend/app/page.js"
      provides: "Wired page with all visualization components, loading states, and error handling"
      contains: "ActivationHeatmaps"
    - path: "frontend/app/components/PredictionSection.js"
      provides: "Enhanced prediction section with ConfidenceChart integration"
      contains: "ConfidenceChart"
  key_links:
    - from: "frontend/app/page.js"
      to: "frontend/app/components/ActivationHeatmaps.js"
      via: "import and prop passing (result.activations)"
      pattern: "activations.*=.*result"
    - from: "frontend/app/page.js"
      to: "frontend/app/components/ConfidenceChart.js"
      via: "import, passed through PredictionSection or directly"
      pattern: "ConfidenceChart|confidence.*=.*result"
    - from: "frontend/app/page.js"
      to: "frontend/app/components/ArchitectureDiagram.js"
      via: "import and render"
      pattern: "ArchitectureDiagram"
    - from: "frontend/app/page.js"
      to: "frontend/app/components/SkeletonLoaders.js"
      via: "conditional render when loading=true"
      pattern: "loading.*Skeleton|Skeleton.*loading"
---

<objective>
Wire all Phase 4 visualization components into the page, add skeleton loading states and error handling, and delete the old placeholder. This is the integration plan that brings everything together.

Purpose: Connect all the individual visualization components into a working page where selecting a digit triggers the full CNN visualization story.
Output: Updated page.js with all components wired, SkeletonLoaders.js for loading states, enhanced PredictionSection with chart.
</objective>

<execution_context>
@/Users/saurabh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saurabh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cnn-visualization/04-RESEARCH.md
@.planning/phases/04-cnn-visualization/04-01-SUMMARY.md
@.planning/phases/04-cnn-visualization/04-02-SUMMARY.md

@frontend/app/page.js
@frontend/app/components/PredictionSection.js
@frontend/app/components/ActivationsPlaceholder.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skeleton loaders and wire all components into page</name>
  <files>frontend/app/components/SkeletonLoaders.js, frontend/app/page.js, frontend/app/components/PredictionSection.js</files>
  <action>
**Step 1: Create SkeletonLoaders**

Create `frontend/app/components/SkeletonLoaders.js` exporting named components:

`SkeletonHeatmapGrid`: Matches ActivationHeatmaps layout. Render 2 sections (one per conv layer), each with a gray label placeholder (`h-5 w-48 bg-gray-200 rounded animate-pulse mb-3`) and a 4-column grid of 8 skeleton squares (`w-16 h-16 bg-gray-200 rounded animate-pulse`). Wrap in card pattern. Use `gap-2` and `grid-cols-4`.

`SkeletonBarChart`: Matches ConfidenceChart layout. Render a div with `h-[280px]` containing 10 flex columns of varying heights with `bg-gray-200 rounded-t animate-pulse`. Use `flex items-end gap-2` and randomized heights (use fixed pseudo-random values like `[30, 15, 20, 10, 25, 12, 8, 65, 18, 22]` as percentages, not Math.random which causes hydration mismatch). Wrap in card pattern with "Confidence" heading.

Both use Tailwind `animate-pulse` for shimmer effect per locked decision.

**Step 2: Enhance PredictionSection**

Modify `frontend/app/components/PredictionSection.js` to:
- Accept additional props: `confidence` (full array), `prediction` (number), and `loading` (boolean)
- Import ConfidenceChart from "./ConfidenceChart"
- Import SkeletonBarChart from "./SkeletonLoaders"
- Keep existing prediction digit and confidence percentage display
- Below the existing content (after the details tooltip), render:
  - If `loading`: `<SkeletonBarChart />`
  - Else if `confidence` and `prediction !== null`: `<ConfidenceChart confidence={confidence} prediction={prediction} />`
  - Else: nothing (initial state before first fetch)
- Remove the card wrapper from ConfidenceChart rendering IF ConfidenceChart already has its own card. Check 04-02-SUMMARY to see if ConfidenceChart includes card wrapper. If it does, render it below PredictionSection as a sibling in page.js instead. The key decision: avoid double card wrapping.

**Step 3: Wire page.js**

Update `frontend/app/page.js`:
- Remove import of ActivationsPlaceholder
- Add imports: ActivationHeatmaps, ArchitectureDiagram, SkeletonHeatmapGrid (from SkeletonLoaders)
- Delete the `<ActivationsPlaceholder />` usage

Page layout order (inside the main flex column):
1. `<Header />`
2. Error banner (existing, keep as-is)
3. `<ImageSection />` (existing, keep as-is)
4. `<ArchitectureDiagram />` -- always visible, static diagram (no loading state needed for static content)
5. `<PredictionSection result={result} loading={loading} />` -- pass loading prop. PredictionSection internally handles showing skeleton chart vs real chart.
6. Activation heatmaps section:
   - If `loading` and no previous result: `<SkeletonHeatmapGrid />`
   - If `loading` and previous result exists: show previous heatmaps (keep stale data visible like ImageSection does)
   - If `result && result.activations`: `<ActivationHeatmaps activations={result.activations} />`
   - If `error` and no result: render a dashed border error placeholder: "Could not load layer activations" with the existing error styling pattern (dashed border, gray text, retry inherits from the global error banner retry)

**Step 4: Delete ActivationsPlaceholder**

Remove `frontend/app/components/ActivationsPlaceholder.js` since it is fully replaced.

```bash
rm frontend/app/components/ActivationsPlaceholder.js
```

Pass `confidence` and `prediction` to PredictionSection from page.js: `<PredictionSection result={result} loading={loading} />` -- PredictionSection extracts what it needs from result internally (result.confidence, result.prediction).
  </action>
  <verify>
- SkeletonLoaders.js exists with SkeletonHeatmapGrid and SkeletonBarChart exports
- Both skeletons use animate-pulse class
- SkeletonHeatmapGrid has grid-cols-4 with 8 skeleton squares
- SkeletonBarChart has 10 bars with fixed heights
- page.js imports ActivationHeatmaps, ArchitectureDiagram, SkeletonHeatmapGrid
- page.js does NOT import ActivationsPlaceholder
- ActivationsPlaceholder.js is deleted
- PredictionSection imports ConfidenceChart
- page.js conditionally renders skeletons when loading, real components when result available
- ArchitectureDiagram rendered without loading gate (static content)
  </verify>
  <done>
- All visualization components wired into page.js with correct data flow
- Skeleton loaders appear during loading with shimmer animation matching component dimensions
- ActivationsPlaceholder deleted and replaced by ActivationHeatmaps
- PredictionSection enhanced with ConfidenceChart
- Error state shows per-section fallbacks when backend unreachable
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete CNN visualization page</name>
  <files>none</files>
  <action>
Human verification of the complete CNN visualization page. Start backend: `conda run -n ml uvicorn main:app --port 8000` from backend/. Start frontend: `cd frontend && npm run dev`. Visit http://localhost:3000.

What was built: Complete CNN visualization page with activation heatmaps (viridis colormap, 2x4 grids), confidence bar chart (Recharts, predicted digit highlighted), architecture diagram (inline SVG flowchart), skeleton loading states, and error handling.
  </action>
  <verify>
1. Open http://localhost:3000 -- page should auto-load a random digit
2. Verify skeleton loaders appear briefly during loading (gray shimmer placeholders)
3. After loading, verify:
   a. MNIST image displays with digit buttons working
   b. Architecture diagram shows 8 boxes (Input through Output) in horizontal flow with arrows, conv layers in blue
   c. Prediction section shows predicted digit and confidence percentage
   d. Confidence bar chart shows 10 vertical bars, predicted digit bar in blue, others gray, percentages above bars
   e. Two activation heatmap grids (one per conv layer), each showing 8 viridis-colored maps in 2x4 layout with layer labels
   f. Heatmaps show viridis colors (purple-to-yellow), not grayscale
4. Click different digit buttons -- all visualizations should update
5. Stop the backend server -- verify error state appears
6. Restart backend -- click Retry, verify recovery
  </verify>
  <done>User confirms all visualizations render correctly, loading states work, error handling functions, and the full CNN story is visible and educational. Type "approved" or describe issues.</done>
</task>

</tasks>

<verification>
- Page renders complete CNN visualization story: image -> architecture -> prediction + confidence -> layer activations
- Skeleton loaders match component dimensions and use animate-pulse shimmer
- All data flows from single API response through to individual components
- Error handling shows meaningful messages when backend is down
- No ActivationsPlaceholder remnants in codebase
</verification>

<success_criteria>
A student can open the page, pick a digit, and see the complete CNN story: what the network looks like (architecture), what it predicts (confidence chart), and what it sees at each layer (activation heatmaps) -- all with smooth loading transitions and clear error handling.
</success_criteria>

<output>
After completion, create `.planning/phases/04-cnn-visualization/04-03-SUMMARY.md`
</output>
