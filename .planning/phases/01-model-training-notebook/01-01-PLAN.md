---
phase: 01-model-training-notebook
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - notebooks/training.ipynb
  - model/mnist_cnn.h5
  - requirements.txt
autonomous: true
requirements:
  - EDU-01
  - EDU-02
  - EDU-03
  - EDU-04
  - EDU-05
  - EDU-06

must_haves:
  truths:
    - "Notebook runs end-to-end without errors"
    - "Trained model achieves >95% test accuracy"
    - "Confusion matrix heatmap is displayed with all 10 digit classes"
    - "Classification report with per-digit precision/recall/F1 is printed"
    - "Training and validation loss/accuracy curves are plotted"
    - "Model is saved as .h5 file that Phase 2 backend can load"
  artifacts:
    - path: "notebooks/training.ipynb"
      provides: "Complete educational CNN training notebook"
      contains: "Conv2D"
    - path: "model/mnist_cnn.h5"
      provides: "Trained CNN model for Phase 2 backend"
    - path: "requirements.txt"
      provides: "Python dependencies for reproducibility"
      contains: "tensorflow"
  key_links:
    - from: "notebooks/training.ipynb"
      to: "/Users/saurabh/Downloads/mnist.pkl.gz"
      via: "gzip + pickle load with encoding='latin1'"
      pattern: "pickle.load.*latin1"
    - from: "notebooks/training.ipynb"
      to: "model/mnist_cnn.h5"
      via: "model.save()"
      pattern: "model\\.save.*mnist_cnn\\.h5"
    - from: "model/mnist_cnn.h5"
      to: "Phase 2 backend"
      via: "Named layers enable activation extraction"
      pattern: "name='conv2d_1'"
---

<objective>
Create and execute the complete MNIST CNN training notebook that teaches CNN concepts from scratch and produces a trained .h5 model file.

Purpose: This is the foundational artifact for the entire project -- the notebook teaches students about CNNs and produces the model that the backend API (Phase 2) will load for inference and activation extraction.

Output: A runnable Jupyter notebook (notebooks/training.ipynb), a trained model file (model/mnist_cnn.h5), and a requirements.txt for reproducibility.
</objective>

<execution_context>
@/Users/saurabh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saurabh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-model-training-notebook/01-CONTEXT.md
@.planning/phases/01-model-training-notebook/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up Python environment and create the complete training notebook</name>
  <files>requirements.txt, notebooks/training.ipynb</files>
  <action>
**Step 1: Environment setup**

Create a `requirements.txt` with:
```
tensorflow
numpy
scikit-learn
matplotlib
seaborn
jupyter
```

Create a Python 3.12 virtual environment and install dependencies:
```bash
python3.12 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

Verify TensorFlow imports successfully: `python -c "import tensorflow; print(tensorflow.__version__)"`. If this fails, troubleshoot -- this is a blocker.

**Step 2: Create notebooks/training.ipynb**

Create the notebook programmatically using `nbformat` (pip install if needed) or by writing the JSON structure directly. The notebook MUST have 7 clearly sectioned parts with rich markdown explanations and code cells.

**Section 1: Introduction (markdown only)**
- Title: "Training a Convolutional Neural Network on MNIST"
- Brief explanation: what is a CNN, what is MNIST, what will we build
- Explain this is for readers with NO ML background
- Describe the architecture we will build (high-level)

**Section 2: Data Loading (EDU-02)**
- Markdown: explain the Nielsen pickle format, why we use local data
- Code cell: imports (gzip, pickle, numpy, matplotlib)
- Code cell: load from `/Users/saurabh/Downloads/mnist.pkl.gz` using `encoding='latin1'`
- Use auto-detection: try 3-tuple first `(train, val, test)`, fall back to 2-tuple
- Print shapes immediately after loading
- CRITICAL: use the exact path `/Users/saurabh/Downloads/mnist.pkl.gz`

**Section 3: Preprocessing (EDU-03)**
- Markdown: explain why reshaping is needed (flat vectors vs 2D images), what the channel dimension is
- Code cell: assert data is already in [0, 1] range (Nielsen data is pre-normalized -- do NOT divide by 255)
- Code cell: reshape all sets from (N, 784) to (N, 28, 28, 1)
- Code cell: display 10 sample images in a 2x5 grid with their labels using matplotlib, titled "Sample Training Images (verify orientation)"
- Markdown: explain what one-hot encoding is (but note we use sparse_categorical_crossentropy so we keep integer labels)

**Section 4: Model Architecture (EDU-03)**
- Markdown: explain each layer type -- Conv2D (what convolution does, what a filter/kernel is), MaxPooling (what pooling does and why), Dense (fully connected), Dropout (regularization concept), Softmax (probability output)
- Per user decision: architecture is LOCKED as Input(28,28,1) -> Conv2D(8, 3x3, relu, name='conv2d_1') -> MaxPool(2x2, name='maxpool_1') -> Conv2D(8, 3x3, relu, name='conv2d_2') -> MaxPool(2x2, name='maxpool_2') -> Flatten -> Dense(128, relu, name='dense_1') -> Dropout(0.25, name='dropout_1') -> Dense(64, relu, name='dense_2') -> Dropout(0.25, name='dropout_2') -> Dense(10, softmax, name='output')
- CRITICAL: all layers MUST have explicit `name=` parameters (Phase 2 needs them for activation extraction)
- Code cell: build model using `tf.keras.Sequential`
- Code cell: `model.summary()` to show parameter counts
- Markdown: explain the summary output (parameter count, output shapes)

**Section 5: Training (EDU-04)**
- Markdown: explain compilation (optimizer, loss function, metrics), what Adam is, what crossentropy means
- Code cell: compile with optimizer='adam', loss='sparse_categorical_crossentropy', metrics=['accuracy']
- Markdown: explain batch size, epochs, validation data purpose
- Code cell: `history = model.fit(x_train, y_train, epochs=15, batch_size=128, validation_data=(x_val, y_val), verbose=1)`
- Code cell: plot training and validation loss curves (side by side subplots)
- Code cell: plot training and validation accuracy curves (side by side subplots)
- Use the exact plotting pattern from RESEARCH.md Pattern: Training Curves

**Section 6: Evaluation (EDU-05)**
- Markdown: explain test evaluation, why we use a held-out test set
- Code cell: `test_loss, test_acc = model.evaluate(x_test, y_test, verbose=0)` and print overall accuracy
- Code cell: `y_pred = model.predict(x_test).argmax(axis=1)`
- Code cell: confusion matrix heatmap using seaborn -- `sns.heatmap(cm, annot=True, fmt='d', cmap='Blues', xticklabels=range(10), yticklabels=range(10))` with axis labels and title
- Markdown: explain how to read the confusion matrix (diagonal = correct, off-diagonal = errors)
- Code cell: `print(classification_report(y_test, y_pred, digits=3))` with explanation
- Markdown: interpret the results -- which digits are hardest to distinguish?

**Section 7: Model Export (EDU-06)**
- Markdown: explain why we save as .h5, how the backend will use this model
- Code cell: `os.makedirs('model', exist_ok=True)` then `model.save('model/mnist_cnn.h5')`
- Code cell: verify by reloading: `loaded = keras.models.load_model('model/mnist_cnn.h5')` and run `loaded.summary()`
- Code cell: verify Phase 2 activation extraction pattern works (build multi-output model from loaded model, predict on one sample, print layer output shapes)
- Markdown: "Next steps: the backend API (Phase 2) will load this model to serve predictions and visualize activations"

**Notebook style notes:**
- Hybrid tone: brief theory intro paragraphs, then annotated code
- Every code cell should have comments explaining non-obvious lines
- Use `## Section Title` markdown headers for clear navigation
- Rich but not verbose -- this is a tutorial, not a textbook
  </action>
  <verify>
- `ls notebooks/training.ipynb` confirms file exists
- `python -c "import nbformat; nb = nbformat.read('notebooks/training.ipynb', as_version=4); print(f'{len(nb.cells)} cells')"` shows the notebook has cells
- `source .venv/bin/activate && python -c "import tensorflow; print(tensorflow.__version__)"` confirms TF is installed
  </verify>
  <done>
- notebooks/training.ipynb exists with all 7 sections (Intro, Data Loading, Preprocessing, Architecture, Training, Evaluation, Export)
- requirements.txt lists all dependencies
- Python 3.12 venv exists with all packages installed
- Every layer in the model definition has an explicit name= parameter
- Data is loaded from /Users/saurabh/Downloads/mnist.pkl.gz (not tf.keras.datasets)
- No double normalization (assert data max <= 1.0, no /255)
- Uses sparse_categorical_crossentropy with integer labels
  </done>
</task>

<task type="auto">
  <name>Task 2: Execute notebook end-to-end and verify all outputs</name>
  <files>model/mnist_cnn.h5, notebooks/training.ipynb</files>
  <action>
Run the notebook end-to-end using the command line:

```bash
source .venv/bin/activate
cd /Users/saurabh/Documents/saurabh/DCS/Selectives/mnist
jupyter nbconvert --to notebook --execute notebooks/training.ipynb --output training.ipynb --ExecutePreprocessor.timeout=600
```

This executes all cells in order and saves the output (including plots) back into the notebook.

**After execution, verify:**

1. **No errors:** The nbconvert command exits with code 0 (no cell raised an exception)
2. **Model file exists:** `ls -la model/mnist_cnn.h5` shows the file exists and has a reasonable size (should be ~100KB-500KB for this small model)
3. **Accuracy check:** Extract test accuracy from the executed notebook output. It MUST be >95%. Expect ~97-98% with this architecture.
4. **Phase 2 compatibility:** The notebook's final verification cell (activation extraction pattern) must have succeeded -- confirms the .h5 model can be loaded and wrapped in a multi-output Model for layer-by-layer activation extraction.

**If accuracy is below 95%:** Investigate -- likely a data issue (double normalization, wrong reshape, label mismatch). Check the notebook cell outputs for training curves -- if loss is not decreasing, there is a fundamental problem.

**If execution fails:** Read the error traceback, fix the notebook cell that failed, and re-run.

**After successful execution:** The notebook file now contains all outputs (plots rendered inline, print statements captured) making it a complete educational artifact.
  </action>
  <verify>
- `ls -la model/mnist_cnn.h5` shows the model file exists
- Notebook executed without errors (nbconvert exit code 0)
- Test accuracy printed in notebook output is >95%
- `source .venv/bin/activate && python -c "from tensorflow import keras; m = keras.models.load_model('model/mnist_cnn.h5'); print('Layers:', [l.name for l in m.layers])"` shows named layers including conv2d_1, conv2d_2, dense_1, dense_2, output
  </verify>
  <done>
- model/mnist_cnn.h5 exists and can be loaded by TensorFlow
- Notebook ran end-to-end without errors
- Test accuracy is >95% (visible in notebook output)
- Confusion matrix heatmap is rendered in the notebook
- Classification report is printed in the notebook
- Training/validation curves are plotted in the notebook
- Model layers have explicit names (conv2d_1, conv2d_2, dense_1, dense_2, output)
- Activation extraction verification cell succeeded
  </done>
</task>

</tasks>

<verification>
1. `notebooks/training.ipynb` exists with executed outputs (plots, metrics visible)
2. `model/mnist_cnn.h5` exists and loads without error
3. Test accuracy >95% (visible in notebook evaluation section)
4. Confusion matrix and classification report present in notebook output
5. Training/validation loss and accuracy curves plotted
6. Model has named layers compatible with Phase 2 activation extraction
7. `requirements.txt` lists all dependencies
</verification>

<success_criteria>
- Jupyter notebook runs end-to-end without errors, producing a trained .h5 model file
- Notebook demonstrates data loading from mnist.pkl.gz, preprocessing, model definition, training with validation, and test evaluation
- Notebook displays scikit-learn confusion matrix and classification report on test data
- Trained model achieves >95% accuracy on MNIST test set
- Model layers are explicitly named for Phase 2 compatibility
</success_criteria>

<output>
After completion, create `.planning/phases/01-model-training-notebook/01-01-SUMMARY.md`
</output>
